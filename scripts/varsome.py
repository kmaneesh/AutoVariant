"""VarSome API lookup (requires API key). Stub until implemented."""

def lookup_varsome(
    chrom: str, pos: int, ref: str, alt: str, *, api_key: str | None = None
) -> dict | str | None:
    """Look up variant in VarSome. Returns None if no key or not implemented."""
    return None
